# Backend base URL the dashboard calls.
# Local dev: leave EMPTY — `npm run dev` proxies /api to http://localhost:8000.
# Production (Vercel/Netlify): set to your deployed backend, e.g.
#   VITE_API_BASE=https://aisha-video-maker.onrender.com
VITE_API_BASE=
